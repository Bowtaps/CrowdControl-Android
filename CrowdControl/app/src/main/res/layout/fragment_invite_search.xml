<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical" android:layout_width="match_parent"
    android:layout_height="match_parent">

    <ListView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/user_search_list_view"
        android:textAppearance="?android:attr/textAppearanceMedium"
        android:layout_margin="15dp"
        android:smoothScrollbar="true"
        android:scrollingCache="false"
        android:hint="Search"
        android:scrollIndicators="right"
        android:layout_below="@+id/invite_search_edit_text"
        android:layout_above="@+id/invite_cancel_button"
        android:layout_centerHorizontal="true" />

    <EditText
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/invite_search_edit_text"
        android:layout_alignParentTop="true"
        android:layout_alignStart="@+id/user_search_list_view"
        android:layout_marginTop="96dp"
        android:hint="Search"
        android:layout_alignEnd="@+id/user_search_list_view" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_margin="15dp"
        android:text="Cancel"
        android:id="@+id/invite_cancel_button"
        android:layout_alignParentBottom="true"
        android:layout_alignParentStart="true" />
</RelativeLayout>